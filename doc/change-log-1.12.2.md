# Список изменений

## Релиз 0.1 (ДАТА)

## [Искусственный ителлект]

- 
- 

## [Генерация блоков]

- 
- 
- 
- 
- 

## [Ландшафт и окружение]

- 

## [Консольные команды]

- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 

## [Техническое]

- 
- 
- 
- 
- 
- 
- 
- 
- 

## [Встроенные модификации (Плагины)]

- 
- 
- 
-  
- 

## [Игровые события и гемплей]

## Концепция сложности биомов



- 
- 

**Примечания:**

- 
- 

## Концпеция выпадения голов с сущностей

Реализована система выпадения голов при убийстве враждебных мобов с учетом:

- Типа убитого моба
- Используемого оружия
- Зачарований на оружии

## Концепция улучшенного дропа вещей со скелетов

Реализована механика выпадения экипировки и стрел при смерти скелетов с учетом:

- Повреждения экипировки
- Шансов выпадения
- Конфигурационных параметров

## Концепция улучшенного дропа вещей с зомби

Реализует механику выпадения поврежденной экипировки при убийстве зомби с конфигурируемыми параметрами

## Концепция никнеймов для сущностей

Реализует систему генерации случайных имен и сообщений о смерти для:

- Зомби (30% шанс получения имени)
- Деревенских жителей (всегда получают имена)

## Концепция разрастания травы

Многопоточная система постепенного естественного разрастания травы вокруг игроков с:

- Контролируемой интенсивностью
- Оптимизированной нагрузкой
- Статистическим мониторингом (Для разработчиков)

## Концепция работы с мультиплеером

Реализует систему отслеживания игроков и управления игровыми событиями, связанными с их подключением/отключением.

- Учет подключенных игроков
- Защита зоны респавна
- Проверка мультиплеерного режима

## Событие, которое отслеживает текущий FPS

Простой оверлей для отображения текущего FPS в правом нижнем углу экрана.

## Обновленная механика работы огня

- Добавляет эффект горения (1 сек) при смерти от огня, даже если моб не горел визуально (Работает только на серверной стороне)
- Делает 70% шанс что стрелы, выпущенные горящими скелетами, будут гореть (5 сек) (Не применяется к зомби и другим стрелкам)
- Автоматически тушит горящих существ с эффектом огнестойкости (Работает в реальном времени)
- Добавляет 30% шанс поджечь цель при атаке голыми руками горящим существом (2 сек)
- Особые правила для огнива:
- - Гарантированное поджигание на 3 секунды
- - Наносит 1 урон прочности огниву
- - Исключает зомби из механики огненных атак

- Все изменения происходят только на серверной стороне
- Учитывает сложность мира при расчете урона
- Использует простую вероятностную систему (tryPercentage)

## Обновленная механика работы факела

- Обрабатывает атаки мобов и игроков, держащих факел (Определяет тип атакующего (зомби/игрок/скелет))
- Если зомби атакует с факелом в руке → поджигает цель на 5 секунд
- Если игрок атакует с факелом в руке → поджигает цель на 5 секунд
- Если скелет атакует с факелом в руке → поджигает цель на 5 секунд
- Особый эффект при разрушении блоков факелом, при разрушении TNT факелом:
- - Создается мощный взрыв (сила 100)
- - TNT уничтожается без дропа
- - Факел превращается в палку (если не в креативе)
- - Отменяется стандартное разрушение блока

## Индивидуальное название игрового окна

- Позволяет кастомизировать заголовок окна Minecraft
- Поддерживает динамические переменные в формате строки

## Концепция 'LootBoxInWorld'

Реализует процедурную генерацию сундуков с лутом разной редкости в мире Minecraft. Сундуки появляются в случайных местах с учетом их уровня редкости.

### Уровни редкости

| Тип сундука   | Шанс появления | Типичное расположение   | Особенности           |
|--------------|----------------|-------------------------|-----------------------|
| Обычный      | 60%            | На поверхности          | Базовые ресурсы       |
| Редкий       | 30%            | Неглубокие пещеры       | Ценные предметы       |
| Легендарный  | 10%            | Глубокие подземелья     | Уникальные артефакты  |

## [Mixins]

- Для коренного мода 'DivineRPG' исправлен спавн морских существ.
- Разрешены несовместимые зачарования.

## [Конфигурации]

**cfg_block_world_generator.cfg** - Создает и загружает конфигурационный файл для генерации различных типов блоков

Управляет параметрами генерации для 4 типов блоков:

- Nether Rack
- Mossy Cobblestone
- Monster Egg
- Emerald Ore

Ключевые параметры для каждого блока:

- chance_spawn - шанс появления блока
- min_height - минимальная высота генерации
- max_height - максимальная высота генерации

**cfg_debug.json** - Конфиг отвечает за включения/отключения дополнительной отладочной информации в процессе разработки.

Управляет двумя параметрами отладки:

- showStats – отображение статистики.
- showLoggingInEventLootBoxInWorld – логирование событий, связанных с лут-боксами в мире.

**cfg_skeleton_drop_item.json** - Создает и загружает конфигурационный файл для управления параметрами дропа предметов скелетами.

Основные параметры:

- break_item (0.15) - Вероятность поломки предмета при дропе
- damage_spread_factor (0.2) - Коэффициент распределения урона

Модификаторы урона по зонам:

- hand_item_damage_factor (0.85) - Множитель урона для предмета в руке
- head_damage_factor (0.9) - Множитель урона в голову
- chest_damage_factor (0.9) - Множитель урона в грудь
- legs_damage_factor (0.9) - Множитель урона в ноги
- feet_damage_factor (0.9) - Множитель урона в стопы

Настройки стрел:

- arrows_to_drops (1-3) - Количество выпадающих стрел (случайное значение)

**cfg_zombie_drop_item.json** - Создает и загружает конфигурационный файл для управления параметрами дропа предметов зомби.

Основные параметры:

- break_item (0.15) - Вероятность поломки предмета при дропе
- damage_spread_factor (0.2) - Коэффициент распределения урона

Модификаторы урона по зонам:

- hand_item_damage_factor (0.85) - Множитель урона для предмета в руке
- head_damage_factor (0.9) - Множитель урона в голову
- chest_damage_factor (0.9) - Множитель урона в грудь
- legs_damage_factor (0.9) - Множитель урона в ноги
- feet_damage_factor (0.9) - Множитель урона в стопы

**cfg_log_file.json** - Конфигурационный файл для управления настройками логгирования.

- log_max_lines (Short.MAX_VALUE) - Максимальное количество строк в лог-файле.

По умолчанию установлено максимально возможное значение для типа short (32,767).

**cfg_main_window_title.json** - Конфигурационный файл для настройки заголовка главного окна приложения.

Основной параметр:

- main_window_title - Текст заголовка главного окна
- По умолчанию: "Minecraft: [Версия мода] + [Название мода]"
- Формируется автоматически из данных DynamicSpawnControlStructure.STRUCT_INFO_MOD
- Поддерживает кастомные значения через конфиг

**cfg_player.json** - Конфигурационный файл для управления параметрами игрока

Основной параметр:

- protected_respawn_player_radius (15) - Радиус защищенной зоны при возрождении игрока (в блоках)
- Определяет безопасную область вокруг точки возрождения, где мобы не могут заспауниться
- Значение по умолчанию: 15 блоков
- Минимальное значение: 0 (отключение защиты)
- Максимальное значение: 32767 (ограничение типа short)

**cfg_synchronization.cfg** - Конфигурационный файл для управления многопоточным обработчиком роста травы.

Основные параметры:

Настройки потока ThreadOvergrowingGrass:

- ticks_between_checks (45) - Количество тиков между проверками роста
- checks_per_player (5) - Максимальное количество проверок на одного игрока за цикл
- growth_chance (0.15) - Шанс роста травы (15%) при благоприятных условиях
- player_radius (36) - Радиус вокруг игрока (в блоках), в котором происходит обработка роста

**cfg_cache_world.cfg** - Конфигурационный файл для управления особыми правилами спавна существ в мире

Основной параметр:

- spawn_peaceful_creatures_at_night (false) - Разрешает/запрещает спавн мирных существ ночью

**plugin_cfg_darkness_forge_1_12_x_0_5_0.cfg** - Конфигурационный файл плагина управления освещённостью и туманом в различных измерениях.

Основные группы параметров:

- Включение эффектов тьмы по измерениям (все true по умолчанию):

- - darknessOverWorld - Тьма в обычном мире
- - darknessNether - Тьма в Нижнем мире
- - darknessEnd - Тьма в Энде
- - darknessDefault - Тьма по умолчанию (резервный параметр)
- - darknessSkyLess - Тьма в подземных/закрытых зонах
- - darknessNetherFog - Туман в Нижнем мире
- - darknessEndFog - Туман в Энде

- Специальные настройки освещения:

- - ignoreMoonLight (false) - Игнорировать лунный свет
- - invertBlacklist (false) - Инвертировать чёрный список (превратить в белый список)

- Система фильтрации:

- - blacklistByID - Чёрный список ID блоков/сущностей (пустой массив по умолчанию)
- - blacklistByName - Чёрный список по именам (пустой массив по умолчанию)

- Факторы фаз луны:
- - moonPhaseFactors [0.6, 0.4, 0.3, 0.2, 0.0, 0.1, 0.2, 0.4] - Множители тьмы для каждой фазы луны

**plugin_cfg_time_control_mod_forge_1_12_2.cfg** - Конфигурационный файл для управления временем в игре.

Основные параметры:

- Настройки длительности времени:

- - day_length_minutes (10) - Длительность дня в реальных минутах
- - night_length_minutes (10) - Длительность ночи в реальных минутах

- Синхронизация с реальным временем:

- - sync_to_system_time (false) - Включить синхронизацию с системным временем
- - sync_to_system_time_rate (20) - Скорость синхронизации (частота обновления)

- Отладка:

- - time_control_debug (false) - Режим отладки (логирование работы мода)

**plugin_cfg_webslinger_1_12_2_2_2_4.cfg** -  Конфигурационный файл для управления механикой паутины и поведения пауков

Основные параметры:

- Механика паутины:

- - block_web_replacement (true) - Замена блоков при создании паутины
- - web_melee_chance (0.15) - Шанс атаки паутиной в ближнем бою (15%)
- - sling_coolDown (45.0) - Время перезарядки (в секундах)
- - sling_inaccuracy (6.0) - Разброс при стрельбе паутиной
- - sling_variance (2.0) - Вариативность траектории

- Настройки ИИ:

- - ai_priority_sling_webs (3) - Приоритет атаки паутиной для ИИ
- - entity_to_attack_web - Привязка приоритетов к типам существ
- - По умолчанию: {"minecraft:spider": 3}

- Особые режимы:

- - sling_webbing (true) - Основной режим стрельбы паутиной
- - sling_webbing_on_web (false) - Стрельба паутиной при нахождении в паутине

## [Игровое кеширование]

Система кеширования сущностей предназначена для эффективного отслеживания и управления мобами и другими живыми сущностями в мире Minecraft. Она состоит из трех основных компонентов:

1. CacheEntityStorage - хранит конфигурационные данные для различных типов сущностей
2. CacheFunctional - предоставляет функциональность для расчетов и операций с сущностями
3. CacheGeneralStorage - основной кеш, хранящий актуальные и буферные данные о сущностях

Основные компоненты:

_**CacheEntityStorage**_

Хранит конфигурацию для различных типов сущностей, включая:

- ResourceLocation - идентификатор сущности
- Флаги per_player и per_chunk - определяют, должен ли лимит сущностей масштабироваться в зависимости от количества игроков или чанков
- max_entity_count - максимальное количество сущностей данного типа
- result - результат обработки (используется для событий)

_**CacheFunctional**_

Содержит вспомогательные методы для работы с сущностями:

- Расчет максимального количества сущностей с учетом количества игроков и загруженных чанков
- Поиск ближайшего игрока
- Подсчет загруженных чанков в области видимости игрока
- Подсчет текущего количества сущностей определенного типа
- Определение валидных чанков для спавна относительно позиции игрока

_**CacheGeneralStorage**_

Основное хранилище кешированных данных, содержащее:

- Два набора данных (актуальный и буферный) для:
- - Животных (EntityAnimal)
- - Враждебных мобов (IAnimals)
- - Водных мобов (EntityWaterMob)
- - Всех живых сущностей (EntityLivingBase)
- - Мапы для быстрого поиска сущностей по имени и ResourceLocation
- - Набор валидных чанков (CACHE_VALID_CHUNKS)
- - Тайминги и счетчики для обновления кеша

_**Принцип работы**_

Инициализация:

- Каждый компонент инициализируется как синглтон через getInstance() и логирует свою инициализацию при наличии аннотации @InitLog.

Обновление кеша:

- Вызывается метод updateCache() в CacheGeneralStorage
- Очищается актуальный кеш
- Определяются валидные чанки для всех игроков
- Все загруженные сущности проверяются и добавляются в соответствующие коллекции

Двойное буферизирование:

- Данные хранятся в двух наборах - актуальном и буферном
- Метод copyActualToBuffer() копирует актуальные данные в буфер
- Это позволяет избежать проблем с параллельным доступом и обеспечить согласованность данных

Очистка кеша:

- Предусмотрены методы cleanActualCache() и cleanBufferCache() для очистки соответствующих наборов данных

Особенности работы:

- Масштабирование лимитов: При расчете максимального количества сущностей учитывается:
- Количество игроков (если per_player = true)
- Количество загруженных чанков (если per_chunk = true)
- Валидные чанки: Определяются на основе позиции игрока и расстояния обзора (viewDistance)
- Типы сущностей: Система различает животных, враждебных мобов, водных мобов и все живые сущности
- Поиск сущностей: Поддерживается поиск по имени и ResourceLocation

Система оптимизирована для многопоточной работы и минимизации нагрузки при частых запросах данных о сущностях.

## [Скриптовые парсеры]

## **parser_event_cache_settings.json**

Парсер ожидает JSON-файл в следующем формате:

```json
[
  {
    "data": 
    {
      "entity": "minecraft:zombie",
      "per_player": true,
      "per_chunk": false,
      "max_entity_count": 20,
      "result": "ALLOW"
    }
  },
  {
    "data": 
    {
      "entity": "cow",
      "per_player": false,
      "per_chunk": true,
      "max_entity_count": 10,
      "result": "DENY"
    }
  }
]
```

Поля

**entity** - Идентификатор сущности. Может быть указан в формате "мод:сущность" или просто "сущность" (в этом случае автоматически добавляется префикс "minecraft:").

Примеры:

- "minecraft:zombie"
- "twilightforest:deer"
- "cow" (будет преобразовано в "minecraft:cow")

**per_player** - Булево значение, определяющее нужно ли учитывать количество игроков при расчете лимита сущностей.

Допустимые значения: true или false.
При значении true максимальное количество сущностей будет масштабироваться в зависимости от количества игроков.

**per_chunk** - Булево значение, определяющее нужно ли учитывать количество загруженных чанков при расчете лимита сущностей.

Допустимые значения: true или false.
При значении true максимальное количество сущностей будет зависеть от количества загруженных чанков.

**max_entity_count** - Целое число, задающее максимальное количество сущностей данного типа.
Минимальное значение: 0 (полный запрет спавна сущности).
Чем выше значение, тем больше сущностей данного типа может существовать в мире.

**result** - Определяет результат обработки сущности. Допустимые значения:

- "ALLOW" - разрешить спавн (если условия соблюдены)
- "DENY" - запретить спавн (игнорируя другие проверки)
- "DEFAULT" - оставить решение на усмотрение других систем

Регистр значения не имеет ("allow" эквивалентно "ALLOW").

## **parser_event_check_spawn.json**

Обработчик сложных JSON-конфигураций, связанных с проверками условий спавна сущностей. Парсер работает с двумя основными разделами:

```json
{
  "templates": 
  {
    // Шаблоны для повторного использования
  },
  "configs": 
  [
    {
      "data": 
      {
        // Основные параметры сущности
      }
    }
  ],
  "data_support": 
  [
    {
      // Дополнительные проверки
    }
  ]
}
```

- Основные конфигурации (configs) - определяют параметры спавна конкретных сущностей
- Дополнительные проверки (data_support) - содержат дополнительные условия для спавна

Основные разделы:

Templates (Шаблоны)

- Необязательный раздел, содержащий шаблоны для повторного использования в конфигурациях.

Configs (Основные конфигурации)

Обязательные поля:

- profile - идентификатор профиля (непустая строка)
- description - описание конфигурации (непустая строка)
- entity_type - тип сущности в формате "мод:сущность"

Опциональные поля:

- priority - приоритет (целое число, по умолчанию 0)
- is_archer - флаг является ли лучником (булево)
- see_sky - может ли сущность видеть небо (булево)
- command_nbt - NBT-данные для команды
- max_height - максимальная высота спавна (целое число)
- min_height - минимальная высота спавна (целое число)
- name - имя сущности (строка)
- equipment - экипировка сущности
- potion - эффекты зелий

Структура equipment:

```json
"equipment": 
{
  "held_item": "...",
  "armor_helmet": "...",
  "armor_chest": "...",
  "armor_legs": "...",
  "armor_boots": "...",
  "has_shield": true/false
}
```

Формат эффектов зелий (potion):

- Каждый эффект задается строкой в формате:
- ```<potion>,<duration>,<amplifier>[,<chance>]```
- Пример: "minecraft:strength,600,0,0.5"

Data Support (Дополнительные проверки)

Содержит дополнительные условия для спавна:

- entity_type - тип сущности
- see_sky - видимость неба (булево)
- potion - эффекты зелий (аналогично основному разделу)

Обработка ошибок

- Парсер выполняет строгую проверку:

- - Обязательные поля profile и description должны присутствовать и быть непустыми
- - Поле entity_type должно содержать существующий тип сущности
- - Эффекты зелий должны быть в правильном формате
- - При обнаружении ошибок генерируется исключение с описанием проблемы

## **parser_event_drop_experience.json**

Предназначен для обработки JSON-конфигураций, определяющих параметры выпадения опыта при убийстве сущностей. Парсер позволяет настраивать:

- Количество базового опыта
- Множители опыта
- Дополнительные бонусы опыта
- Временные интервалы действия настроек
- Результат обработки события

Формат JSON-файла

Файл конфигурации представляет собой массив объектов, каждый из которых содержит настройки для определенной сущности:

```json
[
  {
    "entity": "мод:сущность",
    "use_default_xp": true/false,
    "multi_xp": число,
    "xp": число,
    "adding_xp": число,
    "time_min": число,
    "time_max": число,
    "result": "allow/deny/default"
  }
]
```

Параметры конфигурации

Обязательные параметры:

- entity - идентификатор сущности в формате "мод:сущность" (например, "minecraft:zombie")

**Основные параметры опыта:**

- use_default_xp (булево) - использовать ли стандартное количество опыта для сущности
- Если true: используется стандартный опыт × multi_xp
- Если false: используется явно заданное количество (xp × multi_xp + adding_xp)
- multi_xp (float) - множитель опыта (по умолчанию 1.0)
- xp (int) - базовое количество опыта (только при use_default_xp=false)
- adding_xp (float) - дополнительное количество опыта (только при use_default_xp=false)

**Временные ограничения:**

- time_min (long) - минимальное мировое время для применения настроек
- time_max (long) - максимальное мировое время для применения настроек

Если оба параметра заданы, time_min должен быть меньше time_max

**Результат обработки:**

- result (строка) - определяет обработку события:
- "allow" - разрешить с учетом настроек
- "deny" - запретить выпадение опыта
- "default" - использовать стандартную логику игры

Особенности обработки

- Валидация сущностей:

- - Проверяется существование указанной сущности в реестре
- - Несуществующие сущности пропускаются с записью в лог

- Проверка целостности:

- - При use_default_xp=true обязательно должен быть указан multi_xp
- - Проверяется корректность временного интервала (min ≤ max)

- Типизация данных:

- - Используется универсальный метод getValueFromJson для безопасного извлечения значений
- - Поддерживаются boolean, числовые и строковые значения

- Логирование:

- - Подробно логируется процесс загрузки
- - Фиксируются все ошибки парсинга

Пример конфигурации

```json
[
  {
    "entity": "minecraft:zombie",
    "use_default_xp": true,
    "multi_xp": 1.5,
    "result": "allow"
  },
  {
    "entity": "minecraft:skeleton",
    "use_default_xp": false,
    "xp": 10,
    "multi_xp": 2.0,
    "adding_xp": 5.0,
    "time_min": 1000,
    "time_max": 13000,
    "result": "allow"
  },
  {
    "entity": "minecraft:ender_dragon",
    "result": "deny"
  }
]
```

## **parser_event_drop_item.json**

Обрабатывает JSON-конфигурации, определяющие правила выпадения предметов при убийстве сущностей.

Парсер позволяет настраивать:

- Какие предметы могут выпадать с конкретных мобов
- Диапазоны количества выпадающих предметов
- Шансы выпадения каждого предмета
- Результат обработки для каждого дропа

Формат JSON-файла

Файл конфигурации представляет собой массив объектов, каждый из которых содержит настройки дропа для определенной сущности:

```json
[
  {
    "entity": "мод:сущность",
    "drop": 
    [
      "предмет1", мин_количество1, макс_количество1, шанс1, "результат1",
      "предмет2", мин_количество2, макс_количество2, шанс2, "результат2"
    ]
  }
]
```

Структура параметров

- Обязательные параметры:

- - entity (строка) - идентификатор сущности в формате "мод:сущность" (например "minecraft:zombie")
- - drop (массив) - список параметров дропа, где каждый предмет описывается 5 элементами:
- - Идентификатор предмета (строка)
- - Минимальное количество (целое число)
- - Максимальное количество (целое число)
- - Шанс выпадения (float, 0.0-1.0)
- - Результат обработки (строка: "allow"/"deny"/"default")

Особенности обработки

- Валидация данных:

- - Проверяется существование указанной сущности
- - Длина массива drop должна быть кратна 5
- - Минимальное количество не должно превышать максимальное
- - Шанс выпадения должен быть в диапазоне 0.0-1.0

- Типы результатов:

- - "allow" - разрешить выпадение предмета
- - "deny" - запретить выпадение предмета
- - "default" - использовать стандартную логику игры

- Обработка ошибок:

- - Некорректные записи пропускаются
- - Ошибки записываются в лог
- - Парсинг продолжается для остальных записей

```json
[
  {
    "entity": "minecraft:zombie",
    "drop": 
    [
      "minecraft:rotten_flesh", 0, 2, 1.0, "allow",
      "minecraft:iron_ingot", 0, 1, 0.1, "allow",
      "minecraft:diamond", 0, 1, 0.01, "deny"
    ]
  },
  {
    "entity": "minecraft:skeleton",
    "drop": 
    [
      "minecraft:bone", 0, 2, 1.0, "allow",
      "minecraft:arrow", 0, 2, 0.8, "default"
    ]
  }
]
```

## **parser_event_mob_task_manager.json**

Обрабатывает JSON-конфигурации, определяющие отношения между мобами и их поведение. Парсер позволяет настраивать:

- Враждебные отношения между группами мобов
- Идентификаторы врагов для конкретных мобов
- Паническое поведение мобов
- Сложные взаимные отношения между группами мобов

Формат JSON-файла

Файл конфигурации представляет собой массив объектов с различными типами настроек поведения мобов:

```json
[
  {
    "enemies_to": ["моб1", "моб2"],
    "to_them": ["моб3", "моб4"]
  },
  {
    "enemies_to": ["моб5"],
    "enemy_id": ["моб6"]
  },
  {
    "panic_to": ["моб7"],
    "panic_id": ["моб8"]
  },
  {
    "enemy_id": ["моб9"],
    "them_id": ["моб10"]
  }
]
```

Типы конфигураций

- AddEnemy.Data

- - Настраивает взаимную вражду между группами мобов:
- - enemies_to - массив идентификаторов мобов, которые будут считать врагами
- - to_them - массив идентификаторов мобов, которые будут врагами для указанных в enemies_to

- AddEnemyId.Data

- - Настраивает одностороннюю вражду:
- - enemies_to - массив идентификаторов мобов, которые получат новых врагов
- - enemy_id - массив идентификаторов мобов, которые будут добавлены как враги

- AddPanicToId.Data

- - Настраивает паническое поведение:
- - panic_to - массив идентификаторов мобов, которые будут бояться
- - panic_id - массив идентификаторов мобов, которых будут бояться

- AddEnemyToIdThemToId.Data

- - Настраивает сложные взаимные отношения:
- - enemy_id - массив идентификаторов мобов-агрессоров
- - them_id - массив идентификаторов мобов-жертв

Особенности обработки

**Валидация данных:**

- Проверяется наличие обязательных полей для каждого типа конфигурации
- Массивы должны содержать хотя бы один элемент
- Идентификаторы мобов должны быть в правильном формате

**Логирование:**

- Подробно логируется процесс парсинга каждой конфигурации
- Записываются все загруженные данные
- Фиксируются успешные операции добавления

**Обработка ошибок:**

- Некорректные записи вызывают исключения
- Ошибки ввода-вывода обрабатываются и пробрасываются дальше

Пример конфигурации

```json
[
  {
    "enemies_to": ["minecraft:zombie", "minecraft:skeleton"],
    "to_them": ["minecraft:villager"]
  },
  {
    "enemies_to": ["minecraft:creeper"],
    "enemy_id": ["minecraft:player"]
  },
  {
    "panic_to": ["minecraft:rabbit"],
    "panic_id": ["minecraft:wolf"]
  },
  {
    "enemy_id": ["minecraft:enderman"],
    "them_id": ["minecraft:player"]
  }
]
```

Хранение данных

Результаты парсинга сохраняются в GeneralMobTaskManager:

- addEnemyData - для взаимной вражды
- addEnemyIdData - для односторонней вражды
- addPanicToIdData - для панического поведения
- addEnemyToIdThemToIdData - для сложных отношений

## **parser_event_population_chunk.json**

Обрабатывает JSON-конфигурации, определяющие параметры спавна мобов в чанках. Парсер позволяет настраивать:

- Какие мобы могут появляться в мире
- Веса и частоту их появления
- Размеры групп спавна
- Привязку к определенным биомам
- Максимальное количество в чанке

Формат JSON-файла

Файл конфигурации представляет собой массив объектов с настройками спавна:

```json
[
  {
    "mobs": [
      {
        "mob": "мод:сущность",
        "weight": число,
        "groupcountmin": число,
        "groupcountmax": число,
        "biome": "биом1,биом2",
        "isWater": true/false,
        "spawn_chance_priority": "low/medium/high",
        "max_entities_per_chunk": число
      }
    ]
  }
]
```

Параметры конфигурации

Обязательные параметры:

- mob - идентификатор сущности в формате "мод:сущность"

Основные параметры спавна:

- weight (int) - вес спавна (чем выше, тем чаще появляется) (по умолчанию: 1)
- groupcountmin (int) - минимальный размер группы (по умолчанию: 1)
- groupcountmax (int) - максимальный размер группы (по умолчанию: max(groupcountmin, 1))
- biome (строка) - список биомов через запятую (если не указано - все биомы)
- isWater (bool) - является ли водным мобом (по умолчанию: false)
- spawn_chance_priority (строка) - приоритет спавна (low/medium/high) (по умолчанию: medium)
- max_entities_per_chunk (int) - максимальное количество в чанке (по умолчанию: 1)

Особенности обработки

Валидация данных:

- Проверяется существование указанной сущности
- Проверяется доступность класса сущности
- groupcountmax автоматически устанавливается не меньше groupcountmin
- Поддерживаются как одиночные биомы, так и списки через запятую

Логирование:

- Подробно логируется процесс загрузки каждого моба
- Фиксируются все предупреждения и ошибки
- Записывается итоговый результат парсинга

Хранение данных:

- Создаются записи PopulationChunkStruct.Data для внутреннего использования
- Формируются стандартные Biome.SpawnListEntry для интеграции с игрой

Пример конфигурации

```json
[
  {
    "mobs": 
    [
      {
        "mob": "minecraft:zombie",
        "weight": 100,
        "groupcountmin": 2,
        "groupcountmax": 4,
        "biome": "plains,forest",
        "max_entities_per_chunk": 5
      },
      {
        "mob": "minecraft:skeleton",
        "weight": 80,
        "groupcountmin": 1,
        "groupcountmax": 3,
        "spawn_chance_priority": "high"
      },
      {
        "mob": "minecraft:squid",
        "weight": 50,
        "isWater": true,
        "max_entities_per_chunk": 3
      }
    ]
  }
]
```

## **parser_event_potential_spawn.json**

Обрабатывает JSON-конфигурации, определяющие параметры потенциального спавна мобов в мире. Парсер позволяет настраивать:

- Вероятность появления мобов
- Диапазоны высот для спавна
- Веса и частоту появления
- Размеры групп спавна

Формат JSON-файла

Файл конфигурации представляет собой массив объектов с настройками спавна:

```json
[
  {
    "mobs": [
      {
        "mob": "мод:сущность",
        "weight": число,
        "groupcountmin": число,
        "groupcountmax": число,
        "spawn_chance": число,
        "min_height": число,
        "max_height": число
      }
    ]
  }
]
```

Параметры конфигурации

Обязательные параметры:

- mob - идентификатор сущности в формате "мод:сущность"

Основные параметры спавна:

- weight (int) - вес спавна (по умолчанию: 1)
- groupcountmin (int) - минимальный размер группы (по умолчанию: 1)
- groupcountmax (int) - максимальный размер группы (по умолчанию: max(groupcountmin, 1))
- spawn_chance (float) - шанс спавна (по умолчанию: 0.01)
- min_height (float) - минимальная высота спавна (по умолчанию: 1.0)
- max_height (float) - максимальная высота спавна (по умолчанию: 255.0)

Особенности обработки

Валидация данных:

- Проверяется существование указанной сущности
- Проверяется доступность класса сущности
- groupcountmax автоматически устанавливается не меньше groupcountmin
- Высотные значения проверяются на корректность (min ≤ max)

Логирование:

- Подробно логируются параметры каждого добавленного моба
- Фиксируются все предупреждения о несуществующих мобах
- Записываются итоговые параметры спавна

Хранение данных:

- Создаются записи PotentialSpawnStruct.Data с дополнительными параметрами
- Формируются стандартные Biome.SpawnListEntry для интеграции с игрой

Пример конфигурации

```json
[
  {
    "mobs": 
    [
      {
        "mob": "minecraft:zombie",
        "weight": 100,
        "groupcountmin": 2,
        "groupcountmax": 4,
        "spawn_chance": 0.05,
        "min_height": 0,
        "max_height": 60
      },
      {
        "mob": "minecraft:skeleton",
        "weight": 80,
        "groupcountmin": 1,
        "groupcountmax": 3,
        "spawn_chance": 0.03,
        "max_height": 40
      },
      {
        "mob": "minecraft:ghast",
        "weight": 50,
        "spawn_chance": 0.01,
        "min_height": 100
      }
    ]
  }
]
```

## **parser_event_loot_box_in_world.json**

Основные особенности

- Генерирует сундуки со случайным лутом в мире
- Поддерживает три уровня лута: Обычный (60%), Редкий (30%), Легендарный (10%)
- Шанс появления сундука в чанке - 5%
- Интеллектуальное размещение (на поверхности для обычного, под землей для редкого/легендарного)
- Подробное логирование для отладки

Процесс генерации

- Проверяет, что мир - это overworld (измерение 0)
- Проверяет общий шанс появления (5% на чанк)
- Выбирает уровень лута на основе весов
- Пытается найти валидное место для спавна (5 попыток на чанк)
- Размещает сундук и заполняет соответствующим лутом

Формат JSON

Парсер ожидает JSON файл следующей структуры:

```json
{
  "common": [
    {
      "item": "minecraft:diamond",
      "minCount": 1,
      "maxCount": 3,
      "chance": 0.5
    }
  ],
  "rare": [
    {
      "item": "minecraft:emerald",
      "minCount": 2,
      "maxCount": 5,
      "chance": 0.7
    }
  ],
  "legendary": [
    {
      "item": "minecraft:nether_star",
      "minCount": 1,
      "maxCount": 1,
      "chance": 0.1
    }
  ]
}
```

Свойства предметов:

- item: ID предмета (например "minecraft:diamond")
- minCount: Минимальное количество
- maxCount: Максимальное количество
- chance: Вероятность появления (от 0.0 до 1.0)
